name: myfirst_project
on: push
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Get Code
              uses: actions/checkout@v3
            - name: NPM install
              uses: actions/setup-node@v3
              with:
                node-version: 18
            - name: install npm
              run: npm install  
